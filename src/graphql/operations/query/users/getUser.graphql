query GetUser($id: ID!) {
  user(id: $id) {
    id
    username
    email
    firstName
    lastName
    relatedUser {
      selfIntroduction
      githubUsername
      twitterUsername
      websiteUrl
    }
  }
}
