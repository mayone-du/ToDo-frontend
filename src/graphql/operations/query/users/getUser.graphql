query GetUser($id: ID!) {
  user(id: $id) {
    id
    username
    email
    firstName
    lastName
    relatedUser {
      profileName
      selfIntroduction
      githubUsername
      twitterUsername
      websiteUrl
    }
  }
}
